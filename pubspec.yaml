name: fa_bank
description: FA Bank

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.3+3

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

# fix versions when doing production software
  dio:
  get_it:
  cupertino_icons:
  shared_preferences:
  flutter_bloc:
  graphql_flutter:
  google_fonts:
  intl:
  charts_flutter:
  url_launcher:

dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable:
  json_annotation:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/images/codemate_logo_white.png
    - assets/images/codemate_logo_black.png
    - assets/images/fa-bank.png
    - assets/images/fa-bank-login.png