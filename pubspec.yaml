name: fa_bank
description: FA Bank

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.23+23

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

# fix versions when doing production software
  dio:
  get_it:
  cupertino_icons:
  community_material_icon:
  shared_preferences:
  flutter_bloc:
  google_fonts:
  intl:
  fl_chart:
  url_launcher:
  flutter_money_formatter:
  package_info:
  video_player:
  visibility_detector:
  lipsum:
  camera:
  permission_handler:
  webfeed:
#  path_provider:
#  path:

dev_dependencies:
  flutter_test:
    sdk: flutter

  json_serializable:
  json_annotation:
  build_runner:

flutter:
  uses-material-design: true

  assets:
    - assets/images/fa-logo.png
    - assets/images/shutterstock_111899093.jpg
    - assets/images/shutterstock_1627633462.jpg
    - assets/images/shutterstock_403945477.jpg
    - assets/demo.mp4
    - assets/config.b64
    - assets/config.json
